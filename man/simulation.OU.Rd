% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.OU.R
\name{simulation.OU}
\alias{simulation.OU}
\title{Simulate an Ornstein-Uhlenbeck Process}
\usage{
simulation.OU(
  p.dim,
  m.dim,
  mu,
  Z.matrix,
  H.matrix,
  C.matrix,
  Q.matrix,
  P1,
  N,
  sim.num,
  tn.diff
)
}
\arguments{
\item{p.dim}{Integer. The number of observed variables.}

\item{m.dim}{Integer. The number of latent state variables.}

\item{mu}{Numeric vector of length \code{p.dim}. The mean of the observed process.}

\item{Z.matrix}{Numeric matrix of dimension \code{p.dim} x \code{m.dim}. The observation matrix.}

\item{H.matrix}{Numeric covariance matrix of dimension \code{p.dim} x \code{p.dim}. The observation noise covariance.}

\item{C.matrix}{List of transition matrices \eqn{C(t_n)} indexed by time differences.}

\item{Q.matrix}{List of process noise covariance matrices \eqn{Q(t_n)} indexed by time differences.}

\item{P1}{Numeric covariance matrix of dimension \code{m.dim} x \code{m.dim}. The initial state covariance.}

\item{N}{Integer. The number of time points.}

\item{sim.num}{Integer. The number of simulations to run.}

\item{tn.diff}{Numeric vector of length \code{N-1}. The time differences between consecutive observations.}
}
\value{
A list containing:
\item{ytn}{An array of dimension \code{(N, p.dim, sim.num)}, representing the observed process.}
\item{atn}{An array of dimension \code{(N, m.dim, sim.num)}, representing the latent process.}
}
\description{
This function simulates data from a multivariate Ornstein-Uhlenbeck (OU) process,
generating both latent states and observed values over time.
}
\examples{
# Example simulation (assuming required matrices are defined)
simulation.OU(p.dim = 2, m.dim = 2, mu = c(0, 0), 
              Z.matrix = matrix(c(1, 0, 0, 1), 2, 2), 
              H.matrix = diag(2), 
              C.matrix = list("1" = diag(2)), 
              Q.matrix = list("1" = diag(2)), 
              P1 = diag(2), 
              N = 10, sim.num = 5, 
              tn.diff = rep(1, 9))
}
